included:
  - Sources

excluded:
  - .build
  - Tests

disabled_rules:
  - trailing_whitespace

opt_in_rules:
  - empty_count
  - explicit_init
  - first_where

line_length:
  warning: 130
  error: 200

type_body_length:
  warning: 300
  error: 400

file_length:
  warning: 400
  error: 600
